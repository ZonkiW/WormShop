//------------------------2019-08-27----
步骤：环境搭建，五个包层，jar包导入，页面导入，工具类导入（杂七杂八的东西放在commonutils）,
datasourceutils很强，配置文件导入（servletji数据库的），配置文件就是把一些经常改动的信息提取出来
便于改动的文件，导入SQL文件
xml中的地址用复制的方法不会出错，直接类名复制全类名
MVC中的V可以使用重定向到jsp实现，数据的封装时缺数据时要补全，beanutils.populate
contextpatn获取的是webcontent的地址
jquery与js一样都是前端的东西，获取元素改变属性，两种东西的组合，即元素与属性，jquery、ajax等都可以看成js封装的脚本模块
el、jstl、jsp、servlet是前台与后台交互的，后台是数据的封装与处理，这些都是java语言模块
ajax有个异步的问题，代码执行不是顺序执行，ajax可以自定义校验,ajax的特点是可以添加servlet地址进行动态访问,处理json类型数据
response只是一个请求的响应值，不是一个域
ajax是属于jquery的,$的作用是提取对象,$()下次响应获得元素相关与${}当次响应获得域中值
前台与后台交互使用虚拟地址，后台与前台交互使用request和response的方法
不同网页不同request域
以黑箱子接口思想来编程，考虑传参时要考虑页面返回的问题，考虑数据的作用域与生命周期
发送请求后，程序遍历一遍
servletContext是全局的，包括jsp，是服务器级别的，生命周期重启服务器才能刷新
request中数据的生命周期是一个命令响应周期
href#还是当前页
redis用来做缓存
代码抽取
在html中获得表单数据没有请求是获取不到的
转发是服务器内部操作，重定向是外部操作
路径问题：
几个域的从属问题：